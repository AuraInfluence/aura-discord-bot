FROM node:20-alpine

WORKDIR /app

# Copy the pre-compiled bot bundle (built by esbuild, all deps included)
COPY bots/aura-bot/dist/index.mjs ./dist/index.mjs

CMD ["node", "--enable-source-maps", "dist/index.mjs"]
